![SC_008](SC_figma/SC_008_AD-06_loc-rate-table.png)


check role + care_level_rate của facility của user( user_facilities		)
nếu cập nhập giá đang áp dụng thì sao? => 
 Authorize attribute

| Method | API                                          | Mục đích                                     |
| ------ | -------------------------------------------- | -------------------------------------------- |
| GET    | `/admin/loc-rates`                           | Lấy bảng LOC Rate hiện hành                  |
| PUT    | `/admin/loc-rates`                           | Cập nhật nhiều Level cùng lúc (Save Changes) |
| PUT    | `/admin/loc-rates/{levelId}`                 | Cập nhật một Level                           |
get rate cho trong 1 khoảng tg( trả về 1 hoặc từng nhiều rate tùy trường hợp) *** cần check điều kiện kỹ chỗ này

=> nếu update rate thế này rồi mấy cái bill trong quá khứ tính sao? 
mỗi bill ghi giá riêng?
à bảng này có effective_from -> effective_to
từ đó có thể ra bill chính xác.
=> chỉ được cập nhập cho rate cho tương lai( rate chưa được tính vào invoices) => báo giá trước khả năng sẽ không đúng
=> nếu như vậy thì cũng có trường hợp quên set rate cho 1 khoảng tg??? => xử lý sao????

